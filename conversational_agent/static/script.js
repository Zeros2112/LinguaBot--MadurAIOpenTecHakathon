// static/script.js
function submitQuery() {
    var query = document.getElementById("query").value;

    // Perform AJAX request or update the form action to send the query to the server
    // You may want to use JavaScript to update the chat history without a page reload
    console.log("User Query:", query);

    // Add logic to update chat history or communicate with the server
}
